---
# Déploie CTFd (plateforme de gestion du CTF — scoreboard, challenges, équipes) via Docker Compose.
# Hôtes : ctfd
# Usage : make sc/ctfd

- name: Deploy CTFd
  hosts: ctfd
  gather_facts: true
  become: true
  roles:
    - cedille.docker.install
