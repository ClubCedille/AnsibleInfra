---
# Provision + déploie BIND9 (zones .ctf et .camp) pour l'event CTF.
# Charge les flags depuis playbooks/sc/vars/flags.yml (flag #9 → TXT record sur secret.ctf).
# Hôtes : netservices_dns (inventaire SC)
# Usage : make sc/dns

- name: Provision DNS VMs on Proxmox
  hosts: netservices_dns
  gather_facts: false
  become: false
  module_defaults:
    group/community.proxmox.proxmox:
      validate_certs: false
  roles:
    - cedille.proxmox.vm

- name: Wait for SSH on DNS VMs
  hosts: netservices_dns
  gather_facts: false
  tasks:
    - name: Wait for SSH to be available
      ansible.builtin.wait_for_connection:
        timeout: 120

- name: Deploy DNS
  hosts: netservices_dns
  gather_facts: true
  become: true
  vars_files:
    - vars/flags.yml
  pre_tasks:
    - name: Configure pkgcache apt mirror
      ansible.builtin.include_role:
        name: cedille.pkgcache.apt
    - name: Configure pip proxy
      ansible.builtin.include_tasks: "{{ playbook_dir }}/../includes/pip_proxy.yaml"
  roles:
    - cedille.netservices.dns
