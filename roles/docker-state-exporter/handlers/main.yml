---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart ctf-docker-state-exporter timer
  ansible.builtin.systemd:
    name: ctf-docker-state-exporter.timer
    state: restarted
    enabled: true
