CHALLENGE={{ (group_names | difference(['chall', 'single_instance_challenges'])) | first }}
TEAM={{ ansible_host.split('.')[-1] if 'chall' in group_names else 'shared' }}
INSTANCE={{ inventory_hostname }}
COMPOSE_DIR={{ install_docker_compose_target_dir }}
OUTPUT_PATH={{ docker_state_exporter_output_path }}
