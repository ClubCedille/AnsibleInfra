---
# Pool "générique" : exécution native sur l'hôte, pas de conteneur intermédiaire.
# L'utilisateur github-runner est dans le groupe docker, donc `docker build`/`buildx`
# fonctionne directement dans un `run:` de workflow.
github_runner_labels:
  - "generic"

github_runner_group: "generic"
