---
# Pool "Docker-in-Docker" — pour les jobs de build/push d'images.
# Sidecar docker:dind réel depuis AnsibleRoles#98 (remplace le DooD
# précédent) — voir forgejo_runner_dind_sidecar_enabled dans AnsibleRoles.
vm_disk_size: "80G" # cache de layers Docker plus gourmand que le pool générique

forgejo_runner_dind_sidecar_enabled: true

forgejo_runner_labels:
  - "docker-dind:docker://ghcr.io/catthehacker/ubuntu:act-latest"
